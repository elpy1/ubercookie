"""ubercookie backend — server-side tracking vectors and the observation log.

This package powers an *educational* demonstration of how websites persistently
identify visitors. It deliberately tracks the browser so the frontend can show
the user exactly what is happening — and lets them wipe it all again.
"""
